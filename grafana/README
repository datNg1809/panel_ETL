For Grafana to work:
all the contents of ./grafana should be owned by 472.

sudo chown -R 472 ./grafana

But for git to work:
all the contents should be owned by your user.
sudo chown -R your_username ./grafana
